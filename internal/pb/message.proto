syntax = "proto3";
package proto;

option go_package = "../pb";

message Request{
  uint32 length = 1;
  Message message = 2;
}

message Message {
  string channel = 1;
  int32 operation = 2; // 1. 订阅 2. 发布
}
